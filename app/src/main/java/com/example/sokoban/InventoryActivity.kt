package com.example.sokoban

import androidx.appcompat.app.AppCompatActivity
import com.example.sokoban.gameItems.Item
import com.example.sokoban.gameItems.InventoryService
import com.example.sokoban.players.Player

class InventoryActivity : AppCompatActivity()
/**
{    fun displayInventory() {
        val inventory = inventoryService.getInventory()
        var inventoryText = "Inventory:\n"

        for (item in inventory) {
            inventoryText += "${item.name}: ${item.description} (Uses left: ${item.usesLeft})\n"
        }

        // Display this inventory list in a UI element
        tvGameStatus.text = inventoryText
    }
}*/